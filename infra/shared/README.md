# Shared Terraform

This directory is for shared AWS resources.

Expected resources:

- Route53 records for `buildrlab.com` subdomains.
- GitHub OIDC IAM role.
- Shared VPC/subnets/security groups.
- Shared IAM policy boundaries where appropriate.

