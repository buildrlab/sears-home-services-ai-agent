name: Security CI

on:
  pull_request:
    branches: ["dev", "main"]
  push:
    branches: ["dev", "main"]

permissions:
  contents: read
  security-events: write

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Secret scan placeholder
        run: |
          echo "Secret scanning tool will be wired during Phase 0 hardening."

  dependency-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dependency audit placeholder
        run: |
          echo "Backend and frontend dependency audits will be enforced once lockfiles exist."

